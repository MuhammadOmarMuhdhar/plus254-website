<section class="action-groups">
  <h2 class="section-title">Quick Links</h2>

  <div class="group">
    <span class="group-label">Discover</span>
    <div class="action-grid action-grid--medium">
      <a href="/datasets/" class="action-item">
        <div class="action-header">
          <span class="action-title">Browse Datasets</span>
          <span class="action-arrow" aria-hidden="true">→</span>
        </div>
        <p class="action-desc">37 datasets across the Kenyan economy, agriculture, and governance.</p>
      </a>
      <a href="/about/#how-we-source-data" class="action-item">
        <div class="action-header">
          <span class="action-title">Data Sources</span>
          <span class="action-arrow" aria-hidden="true">→</span>
        </div>
        <p class="action-desc">Where the data comes from and how it is collected and updated.</p>
      </a>
      <a href="/about/#mission" class="action-item">
        <div class="action-header">
          <span class="action-title">Our Mission</span>
          <span class="action-arrow" aria-hidden="true">→</span>
        </div>
        <p class="action-desc">Why +254 exists and what it is trying to do.</p>
      </a>
    </div>
  </div>

  <div class="group">
    <span class="group-label">Developers</span>
    <div class="action-grid action-grid--medium">
      <a href="/api-docs/" class="action-item">
        <div class="action-header">
          <span class="action-title">API Reference</span>
          <span class="action-arrow" aria-hidden="true">→</span>
        </div>
        <p class="action-desc">Full endpoint and parameter documentation with examples.</p>
      </a>
      <a href="/about/#conventions" class="action-item">
        <div class="action-header">
          <span class="action-title">Data Conventions</span>
          <span class="action-arrow" aria-hidden="true">→</span>
        </div>
        <p class="action-desc">How datasets are structured: long-form, snake_case, clean numerics, and more.</p>
      </a>
      <a href="/about/#support" class="action-item">
        <div class="action-header">
          <span class="action-title">Support</span>
          <span class="action-arrow" aria-hidden="true">→</span>
        </div>
        <p class="action-desc">Report issues, suggest datasets, or contribute on GitHub.</p>
      </a>
    </div>
  </div>
</section>

<style>
  .action-groups {
    padding: var(--space-md) 0 var(--space-lg);
  }

  .section-title {
    font-family: var(--font-serif);
    font-size: var(--text-xl);
    font-weight: var(--font-bold);
    font-weight: var(--font-semibold);
    letter-spacing: -0.01em;
    color: var(--color-warm-dark);
    margin: 0 0 var(--space-md);
    line-height: 1.2;
  }

  .group {
    margin-bottom: var(--space-md);
  }

  .group-label {
    font-family: var(--font-sans);
    font-size: var(--text-sm);
    font-weight: var(--font-semibold);
    color: var(--color-warm-light);
    letter-spacing: 0.08em;
    text-transform: uppercase;
    display: block;
    margin-bottom: var(--space-xs);
  }

  .action-grid {
    display: grid;
    gap: var(--space-smd);
  }

  .action-grid--medium {
    grid-template-columns: repeat(3, 1fr);
  }

  .action-item {
    display: block;
    position: relative;
    background-color: var(--color-green-bg);
    border: 1px solid rgba(31, 111, 84, 0.35);
    padding: var(--space-md);
    padding-right: calc(var(--space-md) + 1.25rem);
    text-decoration: none;
    overflow-wrap: break-word;
    word-wrap: break-word;
    hyphens: auto;
    transition: border-color 0.25s var(--ease-out), background-color 0.25s var(--ease-out), transform 0.15s var(--ease-out);
  }

  .action-item:hover {
    border-color: var(--color-green);
    background-color: #66b392;
  }

  .action-item:active {
    transform: translateY(1px);
  }

  .action-item:focus-visible {
    outline: 2px solid #1f6f54;
    outline-offset: 2px;
  }

  .action-header {
    display: flex;
    align-items: baseline;
    gap: var(--space-md);
    margin-bottom: var(--space-xs);
  }

  .action-title {
    font-family: var(--font-sans);
    font-size: var(--text-lg);
    font-weight: var(--font-semibold);
    color: var(--color-warm-dark);
    flex: 1;
  }

  .action-arrow {
    position: absolute;
    top: var(--space-md);
    right: var(--space-md);
    font-family: var(--font-sans);
    font-size: var(--text-sm);
    color: #1f6f54;
    font-weight: var(--font-medium);
    transition: transform 0.2s var(--ease-out);
  }

  .action-item:hover .action-arrow {
    transform: translateX(4px);
  }

  .action-desc {
    font-family: var(--font-sans);
    font-size: var(--text-sm);
    color: var(--color-warm-dark);
    line-height: 1.5;
    margin: 0;
    opacity: 0.75;
  }

  @media (max-width: 768px) {
    .action-groups {
      padding: var(--space-md) 0;
    }

    .group {
      margin-bottom: var(--space-md);
    }

    .action-grid--medium {
      grid-template-columns: 1fr;
    }

    .action-item {
      padding: var(--space-md);
    }
  }
</style>
